declare namespace javax {
  namespace swing {
    namespace plaf {
      namespace synth {
        interface SynthConstants {
          readonly ENABLED: int
          readonly MOUSE_OVER: int
          readonly PRESSED: int
          readonly DISABLED: int
          readonly FOCUSED: int
          readonly SELECTED: int
          readonly DEFAULT: int
        }
      }
    }
  }
}
