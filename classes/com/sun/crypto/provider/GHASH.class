Compiled from "GHASH.java"
final class com.sun.crypto.provider.GHASH {
  com.sun.crypto.provider.GHASH(byte[]) throws java.security.ProviderException;
  void reset();
  void save();
  void restore();
  void update(byte[]);
  void update(byte[], int, int);
  byte[] digest();
}
