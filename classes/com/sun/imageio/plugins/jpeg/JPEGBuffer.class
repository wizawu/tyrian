Compiled from "JPEGBuffer.java"
class com.sun.imageio.plugins.jpeg.JPEGBuffer {
  final int BUFFER_SIZE;
  byte[] buf;
  int bufAvail;
  int bufPtr;
  javax.imageio.stream.ImageInputStream iis;
  com.sun.imageio.plugins.jpeg.JPEGBuffer(javax.imageio.stream.ImageInputStream);
  void loadBuf(int) throws java.io.IOException;
  void readData(byte[]) throws java.io.IOException;
  void skipData(int) throws java.io.IOException;
  void pushBack() throws java.io.IOException;
  long getStreamPosition() throws java.io.IOException;
  boolean scanForFF(com.sun.imageio.plugins.jpeg.JPEGImageReader) throws java.io.IOException;
  void print(int);
}
