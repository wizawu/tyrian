Compiled from "CharacterData.java"
abstract class java.lang.CharacterData {
  java.lang.CharacterData();
  abstract int getProperties(int);
  abstract int getType(int);
  abstract boolean isDigit(int);
  abstract boolean isLowerCase(int);
  abstract boolean isUpperCase(int);
  abstract boolean isWhitespace(int);
  abstract boolean isMirrored(int);
  abstract boolean isJavaIdentifierStart(int);
  abstract boolean isJavaIdentifierPart(int);
  abstract boolean isUnicodeIdentifierStart(int);
  abstract boolean isUnicodeIdentifierPart(int);
  abstract boolean isIdentifierIgnorable(int);
  abstract int toLowerCase(int);
  abstract int toUpperCase(int);
  abstract int toTitleCase(int);
  abstract int digit(int, int);
  abstract int getNumericValue(int);
  abstract byte getDirectionality(int);
  int toUpperCaseEx(int);
  char[] toUpperCaseCharArray(int);
  boolean isOtherLowercase(int);
  boolean isOtherUppercase(int);
  boolean isOtherAlphabetic(int);
  boolean isIdeographic(int);
  static final java.lang.CharacterData of(int);
}
