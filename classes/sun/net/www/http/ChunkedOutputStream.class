Compiled from "ChunkedOutputStream.java"
public class sun.net.www.http.ChunkedOutputStream extends java.io.PrintStream {
  static final int DEFAULT_CHUNK_SIZE;
  public sun.net.www.http.ChunkedOutputStream(java.io.PrintStream);
  public sun.net.www.http.ChunkedOutputStream(java.io.PrintStream, int);
  public boolean checkError();
  public synchronized void write(byte[], int, int);
  public synchronized void write(int);
  public synchronized void reset();
  public int size();
  public synchronized void close();
  public synchronized void flush();
  static {};
}
