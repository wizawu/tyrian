{
  "name": "orsql",
  "version": "0.0.1",
  "main": "src/index.js",
  "types": "src/index.d.ts",
  "scripts": {
    "build": "tsc -d --noUnusedLocals --removeComments --strictNullChecks -p .",
    "watch": "tsc -d --noUnusedLocals --removeComments --strictNullChecks -p . -w"
  },
  "mvnDependencies": [
    "mysql:mysql-connector-java:6.0.5",
    "org.mariadb.jdbc:mariadb-java-client:1.5.7"
  ],
  "dependencies": {
    "jasmine": "^2.5.3"
  }
}
