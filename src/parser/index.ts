

export function parse(
  classPaths: string[],
  interfaceCount: { [_: string]: number },
  classList: string[],
  typeRoot: string
) {
  console.debug(classPaths)
  console.debug(interfaceCount)
  console.debug(classList)
  console.debug(typeRoot)
  console.debug("parse...")
}
